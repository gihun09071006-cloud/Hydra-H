# tests/

Test suites and validation harnesses for HYDRA.

## Purpose

This folder holds everything that verifies HYDRA behaves correctly. It is the
home for:

- Unit and integration tests for engines and interfaces.
- Schema and output validation harnesses.
- Reproducibility checks that confirm identical inputs yield identical results.

Tests are added alongside the engines they cover, keeping the QA layer honest
as the system grows.
